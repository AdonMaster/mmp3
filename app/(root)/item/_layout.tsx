import {Stack} from "expo-router"
import {Text, View} from "react-native"

export default function ItemLayout() {
    return <Stack
        screenOptions={{
            headerShown: false
        }}
    >
    </Stack>
}